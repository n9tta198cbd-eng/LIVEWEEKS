# Быстрый старт

## 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

## 2. Запуск сервера

```bash
cd src
python server.py
```

## 3. Открыть в браузере

Откройте: http://localhost:8000/index.html

## Что было исправлено:

✅ Исправлено несоответствие параметров API (`lifes` vs `lifespan`)
✅ Добавлена поддержка автоматического определения локального/удаленного API
✅ Создан локальный сервер для тестирования
✅ Исправлены пути к файлам
✅ Добавлен файл зависимостей

## Структура:

- `src/index.html` - главная страница
- `src/script.js` - основной JavaScript (автоматически определяет API URL)
- `src/api/generate.py` - Python API для генерации изображений
- `src/server.py` - локальный сервер для тестирования

Все файлы теперь правильно связаны и готовы к работе!
