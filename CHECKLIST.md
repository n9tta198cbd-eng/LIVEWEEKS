# Чеклист для исправления 404 на Vercel

## ✅ Проверено:

1. **Структура файлов:**
   - ✅ `api/generate.py` находится в корне проекта в папке `api/`
   - ✅ `requirements.txt` находится в корне проекта
   - ✅ `vercel.json` находится в корне проекта

2. **Содержимое файлов:**
   - ✅ `api/generate.py` содержит класс `handler(BaseHTTPRequestHandler)`
   - ✅ `requirements.txt` содержит `Pillow==10.4.0`
   - ✅ `vercel.json` настроен правильно

3. **Handler:**
   - ✅ Класс `handler` определен правильно
   - ✅ Метод `do_GET` реализован
   - ✅ Обработка ошибок есть

## 🔧 Что нужно сделать:

1. **Передеплойте проект:**
   ```bash
   vercel --prod
   ```
   Или через Git push, если настроена интеграция

2. **Проверьте в Vercel Dashboard:**
   - Зайдите в проект → Deployments
   - Выберите последний деплой
   - Проверьте вкладку **Functions**
   - Должна быть функция `api/generate.py`

3. **Проверьте логи сборки:**
   - В логах деплоя должна быть строка: `Installing dependencies from requirements.txt`
   - Должна быть строка: `Successfully installed Pillow-10.4.0`
   - Не должно быть ошибок импорта

4. **Проверьте URL:**
   - Правильный URL: `https://mainllac.vercel.app/api/generate?type=life&birth=2009-08-01&lifes=90`
   - **НЕ** используйте `/api/generate.py` - только `/api/generate`

## 🐛 Если все еще 404:

1. **Проверьте, что функция видна в Vercel:**
   - Dashboard → Project → Functions
   - Должна быть `api/generate.py`

2. **Проверьте логи функции:**
   - Dashboard → Deployments → выберите деплой → Functions → api/generate.py → Logs
   - Посмотрите, есть ли ошибки

3. **Попробуйте упростить:**
   - Временно уберите все rewrites из vercel.json
   - Оставьте только functions
   - Проверьте, работает ли `/api/generate`

4. **Проверьте домен:**
   - Убедитесь, что домен `mainllac.vercel.app` правильно настроен
   - Проверьте в Settings → Domains

## 📝 Текущая конфигурация:

- **vercel.json:** использует `functions` и `rewrites`
- **api/generate.py:** использует `BaseHTTPRequestHandler`
- **requirements.txt:** `Pillow==10.4.0`
- **Runtime:** `python3.12`
